# NiceKit

NiceKit 是多租户 Agent + 知识库平台 SDK。

分层:

```text
core / tenancy / llm / kb / agent / capabilities / runtime / api / domain / models / migrations
```

仓库中的 `apps/demo` 是 **Nice Knowledge**，用于演示如何把 NiceKit 组装成一个可运行的知识工作台。
