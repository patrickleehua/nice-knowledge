# nicekit demo 前端环境变量示例。复制为 .env.local 后按需修改。
#
# Next.js 只把 NEXT_PUBLIC_ 前缀的变量注入浏览器包;这里只有一项。

# demo 后端地址(不带 /api/v1,客户端自己拼)。
# 后端启动:cd apps/demo/backend && DEMO_PORT=8020 uv run python run.py
# 后端 CORS_ORIGINS 需包含本前端的地址(默认 http://localhost:3000)。
NEXT_PUBLIC_API_BASE=http://localhost:8020
